# docker compose 用的环境变量。
#
#   cp .env.example .env   然后编辑 .env
#
# ⚠️ .env 里有密钥，已经在 .gitignore 里了 —— 别把它提交上去，也别贴进 issue。

# ── 必填 ────────────────────────────────────────────────────────────
# 访问密码。这扇门后面能用你的 OVH 账户下单、能重装服务器。
# 生成一串随机的：openssl rand -hex 32
API_SECRET_KEY=

# ── 建议填 ──────────────────────────────────────────────────────────
# 数据库加密密钥。留空会在首次启动时自动生成并写进 ./data/.env
#（跟着数据卷走，容器重建不会丢）。
# 显式提供的好处是换机器、迁移、k8s secret 时更可控。
# 丢了这把钥匙，已加密的 OVH 凭据和 Telegram Token 就再也解不开了。
# 生成：openssl rand -hex 32
#
# ⚠️ 不用就让它保持注释状态，别写成 `OVH_DB_KEY=`（空值）。
#    空值会被 compose 当成"已定义"传进容器，而空变量和"没设"对程序是两回事。
#    这里踩过一次：空变量让程序读不到 ./data/.env 里的真密钥，
#    每次启动重新生成一把 —— 之前加密的凭据永久解不开。
# OVH_DB_KEY=

# ── 可选 ────────────────────────────────────────────────────────────
# 宿主机监听地址。默认只绑回环 —— 要远程访问请放在反向代理后面，
# 别直接改成 0.0.0.0 暴露到公网。
BIND_ADDR=127.0.0.1
HOST_PORT=20000

TZ=Asia/Shanghai

# 容器内文件的属主。想和宿主机当前用户对齐：id -u / id -g
PUID=10001
PGID=10001

# 放在反向代理后面时按需设置
# CORS_ALLOWED_ORIGINS=https://ovh.example.com
# TRUSTED_PROXIES=172.16.0.0/12
